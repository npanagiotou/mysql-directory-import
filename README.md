# CSV or GZ directory to MySQL

Store the data of a directory in a MySQL database. Typical use case: Large CSV files such as Kaggle!

## Example

python import.py --dbName nikos --fileType gz --dbUser root --tableDir gzDirectory
 
